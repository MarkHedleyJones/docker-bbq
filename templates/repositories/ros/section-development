## Development
The catkin_workspace can be built from within the container by running `cm`.
A production build uses `catkin_make install` so ROS package components must
have corresponding *install(...)* entries in the relevant CMakeLists.txt file.
